import React from 'react';
import { ContainerWrapper, Heading } from './Home/style';

const NotMatch = () => {
    return (
        <ContainerWrapper>
            <Heading>Not Match </Heading>
        </ContainerWrapper>
    );
};

export default NotMatch;